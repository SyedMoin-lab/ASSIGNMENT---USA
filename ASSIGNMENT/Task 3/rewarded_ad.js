// This function should be connected to your ad SDK and trigger the rewarded ad.
function showRewardedAd() {
    // Placeholder: Replace this with actual code to show the rewarded ad
    alert('Showing Rewarded Ad...');
}

// Modify the popup code to show the ad immediately when the page loads
window.onload = function() {
    showRewardedAd();
};
